import * as _ from 'lodash';
const initialState = {
  requesting: false,
  conversations: [],
};

const reducer = (state = initialState, action: any) => {
  switch (action.type) {
    default:
      return { ...state };
  }
};
export default reducer;
